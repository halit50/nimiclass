Pour lancer le projet, récupérer la base du repo
Ensuite Composer Install
Une fois les dépendances installée, faire make start pour lancer les conteneurs Docker puis make bash pour lancer le bash.
Ensuite pour compiler les assets yarn encore dev ou yarn build
